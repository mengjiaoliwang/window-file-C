#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include<math.h>

int main08()
{


	//if (head == NULL)
	//	return;

	//LinkNode * p_cur = head->next;

	//ForeachLinkList(p_cur);
	//if (p_cur != NULL)
	//	printf("%d\n", p_cur->data);
	return EXIT_SUCCESS;
}